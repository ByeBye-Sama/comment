import styled from 'styled-components';
import {color} from '../../assets/style/variables'

const StyleMain = styled.main`
  min-height: 80vh;
  background: ${color.scale0};
`;

export default StyleMain;
